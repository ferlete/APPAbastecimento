<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Abastecimentos</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>
<body class="container" style="margin-top: 50px;">

    <% if (messages.success) { %>
    <div class="alert alert-success" role="alert"><%- messages.success %></div>
    <% } %>

    <% if (messages.error) { %>
    <div class="alert alert-danger" role="alert"><%- messages.error %></div>
    <% } %>

    <div class="card">
        <div class="card-header">
            <ul class="nav nav-pills w-100">
                <li class="nav-pill active">
                    <a class="nav-link">Listagem de abastecimentos</a>
                </li>

                <li class="nav-pill ml-auto">
                  <a class="nav-link active" href="/">Voltar</a>
                </li>

                <li class="nav-pill ml-auto">
                    <a class="nav-link active" href="/abastecimentos/add">Adicionar Abastecimento</a>
                </li>

                </ul>


        </div>
    <div class="card-body">

        <% if(data.length) { %>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Data</th>
                    <th scope="col">Volume</th>
                    <th scope="col">Hodrometro</th>
                    <th scope="col">Valor/Litro</th>
                    <th scope="col">Placa</th>
                    <th width="200px">Ações</th>
                </tr>
            </thead>
            <tbody>
            <% for(var i = 0; i< data.length; i++) { %>
                <tr>
                    <th scope="row"><%= (i+1) %></th>
                    <td><%= data[i].data%></td>
                    <td><%= data[i].volume%></td>
                    <td><%= data[i].hodometro%></td>
                    <td><%= data[i].valor_litro%></td>
                    <td><%= data[i].veiculo_id%></td>
                    <td>
                        <a class="btn btn-success edit" href="../abastecimentos/edit/<%=data[i].id%>">Alterar</a>
                        <a class="btn btn-danger delete" onclick="return alert('Voce Tem Certeza?')" href="../abastecimentos/delete/<%=data[i].id%>">Excluir</a>
                    </td>
                </tr>
            <% } %>
            </tbody>
        </table>
        <% } %>

        <!-- if result is empty -->
        <% if(!data.length) { %>
            <p class="text-center">Nenhum abastecimento encontrado!</p>
        <% } %>

        </div>
    </div>

</body>
</html>
